
const Button =({type,className,children})=>{
  
    return(
        <button type={type} 
        className={className}
        children={children}
        />
        
    )

}

export default Button;